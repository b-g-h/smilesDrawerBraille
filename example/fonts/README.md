# Braille-Font

Lege hier die Datei **Euro850.ttf** ab, damit die Beispielseite
`example/braille.html` den Font korrekt laden kann.

Falls der Font unter einem anderen Namen vorliegt, passe den
`@font-face`-Pfad in `example/braille.html` entsprechend an.
